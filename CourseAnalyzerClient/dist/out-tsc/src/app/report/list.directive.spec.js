import { ListDirective } from './list.directive';
describe('ListDirective', function () {
    it('should create an instance', function () {
        var directive = new ListDirective();
        expect(directive).toBeTruthy();
    });
});
//# sourceMappingURL=list.directive.spec.js.map